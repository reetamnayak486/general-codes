#include <stdio.h>
int main()
{
    //first make indivisual array elements
    arr[]={4,5,6,6,7,8,5,4,7,7,8};
    int len=sizeof(arr)/sizeof(arr[2]);
    for(int i=0;i<length)
printf("");
    return 0;
}