#include <stdio.h>
int marks_sub(int *ptr, int t)
{

    if (y == g)
    {
        for (y = 0; y < 5; y++)
        {
            sumg = sum + (*(arr + 2y))
        }
    }
    if (t == b)
    {
        for (x = 0; x < 5; x++)
        {
            sumb = sum + (*(arr + 2x + 1))
        }
    }
}

int main()
{
    int arr[10];
    for (i = 0; i < 4; i++)
    {
        scanf("%d", arr[i]);
    }
    scanf('%c', &t);
    marks_sub(arr, t);


    return 0;
}